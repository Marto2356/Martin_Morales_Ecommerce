const NavBar = () => {
    return (
        <nav>
            <a href="#">Link</a>
            <a href="#">Link</a>
            <a href="#">Link</a>
        </nav>
    )
}

export default NavBar